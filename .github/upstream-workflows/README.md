# Upstream workflow reference

These are the unchanged ExpressLRS 4.1.0 workflows. They are kept outside
`.github/workflows/` so this independent source fork cannot run upstream
firmware publishing, R2 cleanup, or issue-management jobs.

The active `chat-source.yml` checks source and runs native tests only.
